apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: allow-use-sharedsecret-entitlement-key
  namespace: builds-test
rules:
  - apiGroups: ["sharedresource.openshift.io"]
    resources: ["sharedsecrets"]
    resourceNames: ["entitlement-key"]
    verbs: ["use"]
